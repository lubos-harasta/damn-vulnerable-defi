testMode: assertion
testLimit: 500000

deployer: '0x10000'
sender: ['0x30000'] # to have a different sender then deployer

#seqLen defines how many transactions are in a test sequence 
# seqLen: 100

# fixes the issue with constructor
# revert, out-of-gas, sending ether to an non-payable constructor
codeSize: 0xffffffffff
corpusDir: './contracts/selfie/echidna/corpus'

# filterBlacklist: true
# filterFunctions:
#   [
#     'EchidnaSelfieGeneric._checkPoolBalance()',
#     'EchidnaSelfieGeneric.receiveTokens(address,uint256)',
#   ]
