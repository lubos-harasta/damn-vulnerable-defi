# to load data generated by etheno to be run in DOCKER instance
initialize: /src/contracts/side-entrance/echidna-etheno/init.json
# 10,000 ether is placed in the EchidnaSideEntranceLenderPool contract.
balanceContract: 10000000000000000000000
# Allow for multi-abi use
multi-abi: true
# allow to require fails, assertion mode needed
testMode: assertion
testLimit: 50000
shrinkLimit: 1000
# accounts
deployer: '0x10000'
sender: ['0x10000']
